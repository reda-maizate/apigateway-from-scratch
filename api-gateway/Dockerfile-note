# Start from a Debian-based image with the latest version of Go installed
# and a workspace (GOPATH) configured at /go.
FROM golang:latest

# Copy the local package files to the container's workspace.
ADD . /go/src/mygrpcservice

# Build the mygrpcservice command inside the container.
RUN go install mygrpcservice

# Run the mygrpcservice command when the container starts.
ENTRYPOINT /go/bin/mygrpcservice

# Document that the service listens on port 50051.
EXPOSE 50051